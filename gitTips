How to add project to existing repo?
git init
git remote add origin <repo-url>
git branch -M master // this renames "master" from "main"
git add .
git commit -m "msg"
git push --set-upstream origin master

git push -u origin master // if git push rejected "non-fast-forward"-


How to pull latest changes from repo in local IDE without pushing own code to repo first?

git stash save
git pull
git stash apply
